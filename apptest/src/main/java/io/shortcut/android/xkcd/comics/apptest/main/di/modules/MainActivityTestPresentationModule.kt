package io.shortcut.android.xkcd.comics.apptest.main.di.modules

import org.koin.dsl.module


val mainActivityTestPresentationModule = module {
//    single<NetworkCallbackX> { NetworkCallbackImpl(get(), get(), get()) }
}